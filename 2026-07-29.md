# 2026-07-29 — Three.js 게임 & Gemini 포트폴리오 프로젝트

## 오늘 한 일
- Claude Code로 싱글 파일(`index.html`) 3D 브라우저 게임 "사라진 태양(빛)" 제작 — Three.js + WebGL, UnrealBloom 후처리, Web Audio API로 배경음/효과음 직접 합성, 진행 상황 `localStorage` 저장까지 구현하고 GitHub Pages로 배포
- "사주 보기" 포트폴리오 프로젝트 제작 — 생년월일·시간·성별을 입력받아 Gemini API로 성격/올해 운세/주의할 점을 생성해주는 서비스

## 배운 점
- 외부 모델·텍스처·사운드 에셋 없이도 절차적 생성만으로 3D 게임 하나를 완성할 수 있다는 것, Web Audio API로 사운드를 코드로 합성하는 방법
- **API 키를 클라이언트(브라우저 JS)에 직접 넣으면 안 되는 이유**를 직접 겪음 — 순수 HTML 한 장으로만 만들면 Gemini API 키가 그대로 프론트엔드 코드에 노출되고, public GitHub 저장소에 커밋하는 순간 키가 유출된다는 것
- 해결 방법으로 FastAPI로 얇은 백엔드를 두어 API 키는 서버(.env)에만 보관하고, 프론트는 `/api/saju` 같은 자체 엔드포인트만 호출하도록 구조를 분리하는 패턴을 실습
- `.env`, `.env.*`는 반드시 `.gitignore`에 넣고 `.env.example`처럼 값이 없는 템플릿만 커밋해야 한다는 것을 실제 프로젝트 세팅에서 적용

## 오전 수업 보충 — 클라이언트-서버 구조 & API/JSON 기초 (자막 정리)

- **정적 웹사이트 vs 동적 웹사이트** — 정적 웹사이트는 코드가 고정되어 항상 같은 내용을 보여주는 것(과거 CD로 배포한 게임과 유사), 동적 웹사이트는 유저 데이터에 따라 계속 바뀌는 것. 이전에 만든 게임에 "실시간 점수판"을 못 넣은 이유는 각자의 게임이 정적 웹파일이라 다른 사람의 플레이 데이터를 공유할 방법이 없기 때문이라는 것
- **클라이언트-서버 구조** — 클라이언트(유저의 디바이스/브라우저)는 보여주기·입력을 담당하고, 서버는 저장·계산·판단을 담당한다. 이 둘이 데이터를 주고받는 구조가 대부분의 서비스의 기본 구조라는 것
- **JSON은 키-값 쌍 구조** — 사전에서 색인으로 바로 단어를 찾는 것과 유사하게, 키를 알면 값에 즉시 접근할 수 있는 데이터 포맷. 클라이언트-서버는 어떤 키가 오갈지 미리 "약속"하고 개발한다는 것
- **API 키 보안(재확인)** — API 키는 사용량 기반으로 과금되는 "인증 크레덴셜"이며, 노출되면 타인이 내 신용카드로 과금되는 API를 무단 사용할 수 있다. 퍼블릭이든 프라이빗이든 GitHub에 절대 올리면 안 되고, 로컬 PC에 `.env` 파일로만 보관해야 한다는 것
