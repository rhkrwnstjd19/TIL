# 2026-08-18 — 변수 스코프와 정렬·2차원 배열 알고리즘

## 오늘 한 일
- 지난주에 남겨둔 파이썬 함수 문제 8·9번을 마무리
- `scope.py` 로 변수가 살아있는 범위를 실습 — 함수 안/밖, 중첩 함수, `global`, 리스트를 함수 안에서 바꿨을 때의 차이를 하나씩 출력해서 확인
- 회문(palindrome) 판별 함수를 6가지 방식으로 구현 — 슬라이싱 `[::-1]`, 뒤에서부터 붙이기, `range(start, end, step)` 로 역순 순회, 앞에 붙여가며 뒤집기, 절반만 비교, 투 포인터
- 정렬 4가지를 직접 구현 — `sort()`/`sorted()` 내장, 카운팅 정렬, 버블 정렬, 선택 정렬
- 2차원 배열(`2nd_dimension.ipynb`) — 중첩 `for` 로 가로/세로 순회, 열 단위 합 구하기, 리스트 컴프리헨션으로 `n*m` 배열 만들기
- 델타 배열(`delta.ipynb`) — `di`/`dj` 로 상하좌우 4방향을 훑는 방식, 격자에서 "2에 인접한 1의 개수" 세기

## 배운 점
- `lst.sort()` 는 리스트 자체를 정렬하고 `None` 을 반환하는 반면 `sorted(lst)` 는 정렬된 **새 리스트**를 반환하고 원본을 그대로 둔다는 것 — 그래서 `result = lst.sort()` 로 받으면 `None` 이 담긴다는 것
- **LEGB 규칙**(Local → Enclosed → Global → Built-in) 으로 변수를 찾는다는 것. 함수 안에서 만든 변수는 밖에서 접근할 수 없고, 함수 안에서 바깥 이름에 값을 대입하면 바깥이 바뀌는 게 아니라 **같은 이름의 지역 변수가 새로 생긴다**는 것. `global` 을 쓰면 바깥 값을 바꿀 수 있지만 알고리즘 문제 풀이에서만 쓰고 개발에서는 쓰지 않는다는 것
- 파이썬은 `if`·`for` 블록에 스코프가 없어서 `if True: value = 30` 뒤에 밖에서 `value` 를 그대로 쓸 수 있다는 것 (다른 언어와 다른 점)
- 리스트는 **값이 아니라 위치(참조)** 로 다뤄진다는 것 — 함수 안에서 `lst[0] = 10000` 처럼 원소를 바꾸면 바깥 리스트가 같이 바뀌지만, `lst = [...]` 로 새로 대입하면 바깥은 그대로라는 것
- **2차원 배열 초기화 함정**: `mat = []` 에 같은 `lst` 를 `n` 번 `append` 하면 모든 행이 **같은 리스트 하나**를 가리켜서 `mat[0][0] = 100` 이 전체 행에 반영된다는 것. 반복할 때마다 `[0]*m` 을 새로 만들어야 하고, 그래서 `[[0]*m for _ in range(n)]` 형태를 쓴다는 것
- 카운팅 정렬은 "값의 범위를 미리 알 때만" 쓸 수 있다는 것 — 1~10 처럼 범위가 정해져 있으면 개수만 세서 순서대로 뱉으면 되지만, 임의의 정수를 정렬하라고 하면 쓸 수 없다는 것
- 회문 판별은 뒤집어서 비교하지 않고 **앞뒤 인덱스를 좁혀오는 투 포인터**로 풀면 절반만 봐도 되고, 다른 글자를 만나는 순간 바로 `False` 로 끝낼 수 있다는 것
- 격자 탐색은 방향을 `di = [0, 0, 1, -1]`, `dj = [1, -1, 0, 0]` 처럼 배열로 빼두면 `for d in range(4)` 한 줄로 사방을 볼 수 있다는 것, 그리고 이동 후보 `ni`, `nj` 가 `0 <= ni < n and 0 <= nj < m` 인지 **반드시 먼저 검사**해야 한다는 것
- 같은 답을 세는 문제라도 **기준을 뒤집으면 중복 처리 방법이 달라진다**는 것 — "1을 찾아 주변에 2가 있으면 세기"는 하나 찾고 `break` 하면 되지만, "2를 찾아 주변 1을 세기"는 다른 2가 같은 1을 다시 세지 않도록 센 칸을 0으로 덮어야 한다는 것

## 다음에 볼 것
- 달팽이 배열(`snail.py`) — 방향을 바꿔가며 2차원 배열 채우기
