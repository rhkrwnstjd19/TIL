# 2026-07-22 — Git & GitHub 기초

## 오늘 한 일
- `git init` → `git add` → `git commit` → `git remote add origin` → `git push` 로 이어지는 기본 흐름을 처음부터 직접 실습
- 로컬 저장소를 만들어 GitHub 원격 저장소에 처음으로 push
- 기초 Markdown 문법(제목, 강조, 목록, 표, 코드 블록, 인용) 정리

## 배운 점
- **Git**은 로컬에서 변경 이력을 관리하는 버전 관리 시스템이고, **GitHub**은 그 이력을 온라인에 올려 공유하는 서비스라는 개념 차이를 이해
- 자주 쓰는 흐름은 결국 `수정 → git add → git commit → git push` 한 줄로 요약된다는 것
- 브랜치 관련 명령(`git branch`, `git checkout -b`, `git merge`)과 협업 흐름(Fork/Clone → 브랜치 생성 → commit & push → Pull Request → Merge)의 전체 그림
- `git remote add origin`을 두 번 실행하면 에러가 난다는 것, 원격 주소를 다시 잡으려면 `git remote set-url`을 써야 한다는 실습 중 겪은 실수
