# 2026-08-03 — Next.js + Vercel + Supabase 풀스택 배포 (MCP 연동)

## 오늘 한 일
- CSR/SSR 복습 후 Next.js가 왜 나왔는지(페이지별로 CSR/SSR을 선택하는 하이브리드 프레임워크) 정리
- CRUD·SQL(RDB) 개념을 인스타그램 예시(게시물/댓글/좋아요/팔로우)로 정리
- Claude Code(자연어) → Next.js 프로젝트 생성 → GitHub 레포 push → Vercel MCP 연결 → 원클릭 자동배포까지 실습
- Supabase를 MCP로 Claude Code에 연결해 테이블 생성·데이터 삽입 → 프론트엔드(메인 페이지)에 실시간 연동까지 완료
- "맛집 도장깨기" 미니 서비스 기획 및 개발 착수(자정 제출 과제)

## 배운 점
- **CSR/SSR/Next.js** — CSR은 브라우저가 JS 뭉치를 받아 렌더링(SEO 약점), SSR은 서버가 완성된 HTML을 내려줌(SEO 유리). Next.js는 페이지 단위로 두 방식을 섞어 쓸 수 있게 해주는 프레임워크이고, 이 구조에서 백엔드는 API 서버 역할만 전담한다
- **기술 스택 선택 기준** — ML/AI 커스터마이징이 서비스 핵심이면 백엔드는 Python(FastAPI), 아니면 프론트(JS)와 언어를 통일해 팀 전체를 자바스크립트로 맞추는 편(Node.js)이 유리하다. "이유 없이 기술을 쓰지 않는다"가 원칙
- **CRUD** — DB와의 모든 상호작용은 Create/Read/Update/Delete 네 가지로 분류된다. 예: 게시물 업로드=C, 댓글=C, 좋아요=C, 좋아요 취소=보통 Delete로 구현(업데이트가 아니라 행 자체를 지우는 형식)
- **RDB vs NoSQL** — RDB(MySQL·PostgreSQL 등)는 엑셀 같은 테이블 형식, NoSQL은 JSON 형식. 테이블의 속성(컬럼)은 정답이 없고 서비스 기획자가 직접 정의해야 한다
- **Supabase** — "백엔드용 Vercel" 같은 서비스로, DB 생성 + 각 테이블에 대한 CRUD API를 자동 제공한다. 간단한 서비스는 백엔드 서버 없이 프론트+Supabase만으로도 구현 가능하다(복잡한 ML/AI 로직은 별도 백엔드로 분리하는 게 일반적 운영 방식)
- **MCP 재확인** — AI 에이전트가 기본적으로 못 하는 작업(Vercel 배포, Supabase 조작)을 `claude mcp add`로 연결해 자연어 지시만으로 가능하게 만든다
- **에이전틱 코딩과 개발 To-Do 문서** — "그냥 구현해줘"는 안 된다. 상세 기획서 → 구현 기획 문서 → 최소 200단계 이상의 개발 체크리스트를 만들어 단계별로 문서를 갱신하며 진행시켜야 세션이 끊겨도 이어갈 수 있다
- **로컬/글로벌 하네스 이중 구조** — 로컬 하네스(프로젝트 폴더)에는 이번 프로젝트 기획 내용을, 글로벌 하네스에는 개인 배경·도메인 지식을 넣어 AI가 더 맞춤화된 답을 하게 한다

## 실습 / 명령어
```text
# Claude Code에 자연어로 지시한 예시
"현재 위치에 넥스트 프로젝트를 하나 만들어줘. 다른 폴더 만들지 말고."
"깃헙 레포지토리 만들고 push 해줘. 이거 버셀에 배포할거야."
"이곳 버셀에 배포해줘."
"코드 변경했으니까 커밋하고 push 해줘."
"수퍼베이스 사용해서 맛집 테이블 하나 만들고 거기에 음식 세개만 넣어줘."
"우리 메인 페이지에서 수퍼베이스 음식목록이 보이도록 연결해줘."
```
```bash
# Vercel / Supabase MCP 등록 (Git Bash 관리자 권한)
claude mcp add   # 각 서비스 공식 문서의 install 커맨드를 그대로 복사해 실행
claude
/model   # 가장 낮은 모델(Haiku)로 변경 — 토큰 절약
/mcp     # 등록된 MCP 선택 후 Authenticate
```

## 막혔던 점
- Claude Code CLI가 설치 안 된 상태에서 `claude` 명령이 인식되지 않은 경우 → `npm install`로 설치 후 재시도해 해결
- 기존 레포지토리 하위에 새 프로젝트를 만들어 Vercel이 "중복 레포"로 인식한 경우 → 새 레포 + 새 폴더를 별도로 생성해 해결

## 다음에 볼 것
- 오늘 배운 기술 스택으로 "맛집 도장깨기" 서비스 개발 이어가기(내일도 실습 지속)
- 멀티 세션 오케스트레이션(메인 에이전트가 서브 에이전트에게 작업을 위임하는 구조) — 아직 실습은 안 함, 다음에 살펴볼 주제
