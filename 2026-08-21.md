# 2026-08-21 — API 호출 리팩터링·공공데이터 미세먼지 API, 파이썬 클래스

## 오늘 한 일
- TMDB 실습 마무리 (문제 7·8)
  - 흩어져 있던 요청 코드를 `fetch_tmdb_api(path, my_params)` 하나로 묶어 리팩터링
  - 평점 최고 영화의 출연진(`movie/{id}/credits` 의 `cast`) 조회
  - 영화의 `genre_ids` 를 장르 이름으로 바꾸는 문제를, 이중 반복 → `break` 추가 → **장르 매핑 딕셔너리** 순으로 개선
- **공공데이터 포털 미세먼지 API**(한국환경공단 에어코리아) 실습(`api/dust.ipynb`)
  - 시도별 실시간 측정정보에서 서울의 초미세먼지(`pm25Value`) 최저 측정소 찾기
  - 504 응답을 만나 재시도 로직과 응답 저장(`data.json`) 두 가지 대응책을 구현
  - 측정소 이름(`stationName`)을 키로 하는 딕셔너리로 데이터 재구성
  - 종로구의 한 달치 데이터 조회 (`dataTerm: MONTH`)
- 공개 API 목록 정리(`api/api_example.md`) — 열린데이터광장, 문화공공데이터광장, 공공데이터포털, 업비트 등
- **파이썬 클래스** 시작(`python_class/`) — 클래스 변수와 인스턴스 변수, 매직 메서드, `@property`, `@classmethod`, 데코레이터, 상속과 `super()`, 다중 상속
- 클래스 연습문제 3문항 풀이 (`Student`, `Calculator`, `Book`)

## 배운 점
- **반복 횟수를 세어보면 자료구조 선택의 효과가 눈에 보인다**는 것 — 영화 20편의 장르 id를 장르 목록과 대조할 때, 이중 반복만 쓰면 1102회, 찾는 즉시 `break` 하면 429회, `{id: name}` 매핑 딕셔너리를 미리 만들어두면 77회로 줄었다는 것. 딕셔너리 조회가 `O(1)` 이라 매번 훑을 필요가 없어지기 때문이라는 것
- 상세 정보가 필요하다고 **영화마다 상세 API를 호출하면 요청 수가 폭발한다**는 것 — 장르 목록처럼 한 번에 받아올 수 있는 엔드포인트(`genre/movie/list`)가 있으면 그걸 한 번 받아서 매칭하는 쪽이 낫다는 것
- 엔드포인트마다 함수를 만들지 않고 **경로와 파라미터만 받는 공통 호출 함수**를 하나 두면, URL 조립·토큰 주입·`raise_for_status()` 가 한 곳에 모여 중복이 사라진다는 것
- **공공데이터 API 트러블슈팅 (504)**: 미세먼지 API가 `504` 로 실패했는데, 500번대는 서버 쪽 문제라 코드를 고쳐도 해결되지 않는다는 것. 그래서 ① `time.sleep(1)` 을 두고 정해진 횟수만큼 자동 재시도하거나, ② 한 번 성공한 응답을 `data.json` 으로 저장해두고 그걸 API 대신 읽는 두 가지 대응이 가능하다는 것 — 특히 ②는 수업·개발 중에 남의 서버 상태에 발목 잡히지 않게 해준다는 것
- **API 응답의 숫자가 문자열로 온다**는 것 — `pm25Value` 가 `str` 이라 그대로 비교하면 안 되고, 게다가 결측치가 `'-'` 로 들어와서 `int()` 변환 전에 걸러내야 한다는 것. 그래서 최솟값의 시작값을 `items[0]` 으로 잡는 흔한 방법이 여기서는 통하지 않고 `float('inf')` 로 두어야 했다는 것
- 공공데이터 포털의 `serviceKey` 는 이미 URL 인코딩된 형태로 발급되므로, `params` 로 넘길 때는 `urllib.parse.unquote()` 로 되돌려야 이중 인코딩이 안 난다는 것
- 리스트로 받은 데이터를 특정 키로 자주 찾아야 하면 `{stationName: station}` 처럼 **딕셔너리로 재구성**해두는 게 접근이 쉽다는 것
- 응답을 파일로 남길 때는 `json.dump(data, f, ensure_ascii=False, indent=4)` 로 저장해야 한글이 깨지지 않고 읽을 수 있게 남는다는 것
- 클래스 변수는 모든 인스턴스가 공유하지만, `p1.population = 100` 처럼 **인스턴스 쪽에 대입하면 공유값이 바뀌는 게 아니라 그 인스턴스만의 변수가 새로 생긴다**는 것
- `__str__` 은 `print()` 할 때, `__len__` 은 `len()` 할 때, `__gt__` 는 `>` 비교를 할 때 불리는 매직 메서드라는 것 — 즉 내장 함수·연산자의 동작을 클래스가 정의할 수 있다는 것
- `@property` 를 붙이면 메서드를 **괄호 없이 속성처럼** 읽을 수 있고, `@X.setter` 로 값을 넣을 때 유효성 검사를 끼워넣을 수 있다는 것
- `@classmethod` 의 `cls` 는 인스턴스가 아니라 **클래스 자신**을 가리켜서, 인스턴스를 만들지 않고 `Person.get_population()` 처럼 호출할 수 있다는 것
- 함수도 값이라서 리스트에 담아 `lst[0]()` 로 호출할 수 있고, 데코레이터는 이 성질을 이용해 원래 함수를 감싼 새 함수를 돌려주는 것이라는 것
- 상속에서 자식이 같은 이름의 메서드를 정의하면 부모 것을 덮어쓰고(오버라이딩), `super().__init__()`·`super().메서드()` 로 부모 동작을 재사용한 뒤 내용을 덧붙일 수 있다는 것

## 다음에 볼 것
- 클래스 상속 연습문제(`inheritance_problem.ipynb`) 3문항 — 아직 문제만 받아두고 풀지 않았다
