# 2026-08-20 — requests로 API 호출하기, .env로 키 분리, TMDB 실습

## 오늘 한 일
- 콜스택(`callstack.ipynb`) — 함수가 호출되면 쌓이고 반환하면 빠지는 순서를 출력으로 확인
- `requests` 로 HTTP API 호출 실습(`api/python_api.ipynb`) — 환율 API로 시작해 `jsonplaceholder` 로 GET·POST·PATCH·PUT·DELETE, `params` 로 필터링, 중첩 리소스 조회까지
- 요청 실패 처리 — `timeout`, `raise_for_status()`, `Timeout`/`HTTPError`/`RequestException` 분기
- `python-dotenv` 로 `.env` 에서 키를 읽는 연습(`env_prac.py`)과 `.env.example` 작성
- **TMDB API 실습** — 두 가지 인증 방식(쿼리 파라미터 `api_key` vs 헤더 `Authorization: Bearer <token>`)을 각각 호출해보고, 문제 1~6 풀이
  - 현재 상영작 중 평점 최고 영화 찾기 (직접 반복 → `max(key=...)` 로 개선)
  - 인기작 중 평점 최고 영화 찾기, 그 과정을 `get_movies()`·`get_best_movie()` 함수로 분리
  - `title`/`vote_average`/`release_date` 만 담은 리스트 만들기 (반복문 → 리스트 컴프리헨션)
  - 여러 페이지를 합쳐 40개 이상 모은 뒤 평점순 정렬
  - 평점 최고 영화의 상세 정보에서 수익(`revenue`), 포스터 경로(`poster_path`) 조회
- `max` 함수에 `key` 를 넘기는 방법을 따로 정리(`algorithm/max.ipynb`)

## 배운 점
- HTTP 메서드는 목적에 따라 나뉜다는 것 — 조회는 `get`, 생성은 `post`, 일부 수정은 `patch`, 전체 교체는 `put`, 삭제는 `delete`
- 요청에 조건을 붙일 때 URL 뒤에 `?userId=2` 를 직접 이어붙이는 대신 `requests.get(url, params={"userId": 2})` 를 쓰면 인코딩까지 알아서 처리된다는 것
- **`raise_for_status()` 를 부르지 않으면 404·500 이어도 예외가 나지 않는다**는 것 — 서버에 연결은 됐으니 응답 자체는 정상 도착한 것이고, 상태 코드는 `response.status_code` 에 담겨 있을 뿐이라는 것. 클라이언트 입장에서는 데이터가 안 온 게 문제 상황이므로 명시적으로 예외로 바꿔줘야 한다는 것
- `Timeout`(응답이 안 옴)과 `HTTPError`(응답은 왔지만 4xx·5xx)는 원인이 달라서 따로 잡아야 하고, `RequestException` 은 그 둘을 포함하는 상위 예외라 마지막에 두면 나머지를 받아준다는 것
- API 인증 방식이 서비스마다 다르다는 것 — TMDB는 **쿼리 파라미터에 `api_key` 를 넣는 방식**과 **헤더에 `Authorization: Bearer <token>` 을 넣는 방식**을 둘 다 지원하고, 헤더 방식은 `requests.get(url, headers=...)` 로 넘긴다는 것
- API 키는 코드에 직접 쓰지 않고 `.env` 에 두고 `load_dotenv()` + `os.getenv("KEY")` 로 읽는다는 것. `.env` 는 저장소에 올리지 않고, 대신 **값이 빈 `.env.example` 을 올려서 어떤 키가 필요한지만 알려준다**는 것
- 최댓값을 찾을 때 변수 두 개(`max_value`, `best_movie`)를 굴리며 직접 비교하는 대신 `max(results, key=lambda movie: movie['vote_average'])` 한 줄로 끝낼 수 있다는 것 — `key` 는 "무엇을 기준으로 비교할지"를 알려주는 함수라는 것
- `sorted(..., key=..., reverse=True)` 로 내림차순 정렬이 되고, 여러 페이지 결과를 합칠 때는 `many_movies.extend(movies)` 나 `+=` 를 쓴다는 것
- 함수 매개변수에 값을 대입해두면(`def get_movies(what="now_playing", page=1)`) **기본값**이 되어 인자를 안 줘도 동작한다는 것
- 함수 안에서는 `raise_for_status()` 로 예외를 올리기만 하고 `try`/`except` 처리는 호출하는 쪽에서 하면, 함수가 "데이터를 가져오는 일"에만 집중하게 된다는 것
