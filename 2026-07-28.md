# 2026-07-28 — Claude Code로 이력서 만들기

## 오늘 한 일
- Claude Code를 처음 설치하고 `CLAUDE.md`로 프로젝트별 지침(페르소나, 호칭 등)을 지정하는 방법 실습
- Markdown으로 작성한 이력서 내용을 Claude Code에게 전달해 스타일이 적용된 HTML 이력서로 변환
- `gh` CLI(`gh auth`, `gh repo`, `gh api`)로 새 저장소를 만들고 Claude Code가 대신 커밋·푸시하도록 권한을 설정

## 배운 점
- 프로젝트 루트의 `CLAUDE.md`는 그 폴더에서 작업할 때 Claude Code가 항상 참고하는 지침 파일이라는 것 — 전역 설정(`~/.claude/CLAUDE.md`)과 프로젝트별 설정이 함께 적용된다는 구조를 이해
- Markdown → HTML 변환처럼 "내용은 내가 정리하고, 스타일링/마크업은 AI에게 맡기는" 협업 방식이 꽤 효율적이라는 것
- `.claude/settings.local.json`에 `permissions.allow` 목록을 등록해두면 특정 Bash 명령(`git add`, `git commit`, `gh repo` 등)을 매번 승인하지 않아도 되게 만들 수 있다는 것
- 저장소를 공개(public)로 만들 때는 이력서 안의 개인정보(연락처, 이메일 등) 노출 여부를 미리 점검해야 한다는 것

## 오전 수업 보충 — CLAUDE.md 메모리 구조 & Git 컨벤션 (자막 정리)

- **CLAUDE.md 로컬 vs 전역(글로벌) 메모리** — 로컬 메모리(프로젝트 폴더의 `CLAUDE.md`)는 그 프로젝트에만 적용되는 설명(프로젝트 개요, 폴더 구조 등, 보통 200줄 내외)을 담고, 전역 메모리(`~/.claude/` 경로)는 모든 프로젝트에 공통 적용되는 지침(사용자 개인 정보·말투 규칙 등)을 담는다. 매 세션 시작 시 전역 메모리 → 로컬 메모리 순으로 읽힌다
- **하네스 엔지니어링(=컨텍스트 엔지니어링)** — CLAUDE.md에 모든 걸 한 번에 나열하기보다, 자주 안 쓰는 긴 지침은 별도 파일로 분리해두고 CLAUDE.md에서 필요할 때만 참조하게 구조를 잡는 것 — 컨텍스트가 앞뒤는 잘 기억하고 중간은 흐려지는 경향이 있어 구조화가 중요하다는 것
- **좋은 커밋 메시지 구조** — 제목 한 줄(타입: 내용, 예 `feat. 로그인 구현`) + 빈 줄 + 본문 상세 설명. 타입은 feature(feat)/bugfix/docs/comment 등으로 구분
- **바이브 코딩 3원칙** — ① 한 번에 하나씩만 시킬 것 ② 에러/이상한 화면은 스크린샷으로 캡처해서 그대로 붙여넣고 수정 요청할 것 ③ 완벽보다 완성이 먼저
