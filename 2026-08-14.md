# 2026-08-14 — 에러 분류 재설계와 Gemini 모델 폐기, 파이썬 함수

## 오늘 한 일
- 미니프로젝트(조달 브리핑 서비스): 배포판에서 AI 검색이 불능임을 발견하고 원인을 분석, 모델 호출 실패를 사용자가 할 일이 갈리는 단위로 분류하는 `model-error.ts`를 작성·배포
- 환경변수 드리프트 검사 도구를 새로 만들고, 검토 라운드의 잔여 항목을 최종 대조
- 파이썬 함수(Function) 문제 8문항 중 1~6번을 풀이(등급 판정, 홀짝 판별, 계산기, 이름 필터링, 회원가입 검증, 장바구니 결제금액 계산)

## 배운 점
- HTTP 상태값처럼 구조화된 신호로만 실패를 분류해야 안전하다는 것 — 낱말 매칭 방식(`quota`, `404` 등을 에러 메시지에서 그대로 찾는 방식)은 사용자가 입력한 검색어에 우연히 같은 낱말이 섞이면 오분류가 난다는 것(예: "404 오류 공고 찾아줘"라는 검색어 자체가 잘못 분류됨)
- **에러 메시지 분류 트러블슈팅**: 배포판에서 AI 검색이 실패했는데, 처음엔 "배포판 API 키가 로컬과 다르다"고 단정했지만 이전 로그의 값 길이가 오늘 로컬과 정확히 일치해 그 단정을 철회하고 "제공자 쪽 사용 한도로 거절된다"까지만 확정하는 것으로 결론을 좁혔다
- 구글 Gemini 모델은 예고 없이 폐기(deprecated)될 수 있다는 것 — 실제로 이전에 쓰던 모델 세 개가 모두 `404 NOT_FOUND`로 응답했고, 최신 모델로 바꾸자 정상 응답했다는 것을 실측으로 확인
- 모델 실패는 "재시도하면 되는지"로 나눠야 사용자에게 정직한 안내가 된다는 것 — 설정을 고쳐야 하는 실패, 모델명 자체가 잘못된 실패, 사용량 한도로 인한 실패, 진짜 일시적 실패를 구분해서 각각 다른 안내 문구를 쓴다는 것
- 함수로 리팩터링할 때 `if`/`elif` 체인을 딕셔너리 매핑(`.get()`)으로 바꿀 수 있다는 것, 그리고 하나의 함수가 여러 일을 하면 더 작은 함수로 쪼갤 수 있다는 것

## 다음에 볼 것
- 파이썬 함수 문제 7·8(정수 ID 정형화, 알림 대상자 추출)
