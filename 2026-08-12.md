# 2026-08-12 — 파이썬 리스트(List)와 반복문

## 오늘 한 일
- 컨테이너 자료형 개념 학습 후 리스트(List) 이론 학습 — 가변/불변 컨테이너 구분, 인덱싱·슬라이싱, 멤버십 연산자, 리스트 컴프리헨션, 자주 쓰는 메서드
- 반복문(`for`/`while`) 이론 학습 — `range`, `break`/`continue`/`pass`
- 리스트 관련 문제(구구단·배수 판별·조건 필터링·소수 판별 등)를 problem05까지 진행

## 배운 점
- 컨테이너의 가변/불변은 내장함수 `id()`로 구분할 수 있다는 것 — 리스트처럼 가변 컨테이너는 원소를 삽입해도 `id()`가 그대로 유지되지만, 튜플처럼 불변 컨테이너는 `+=` 연산 후 `id()`가 달라진다는 것(새 객체가 재할당되는 것)
- 슬라이싱은 `[start:end:step]` 형식이며 `end` 인덱스는 포함되지 않는다는 것 — `step`에 음수를 주면 반대 방향으로 자르고, `[::-1]`은 컨테이너를 뒤집는 관용적 표현이라는 것
- 리스트 컴프리헨션은 `for` 반복 + `append`를 한 줄로 축약한 것이라는 것 — `[i for i in range(1,11) if i%2==0]`처럼 조건까지 필터링에 포함시킬 수 있다는 것
- `while True` + `break` 조합으로, 조건이 매번 바뀌는 상황도 루프 안에서 직접 검사해 안전하게 종료할 수 있다는 것 — 일반 `while 조건:` 보다 유연하다는 것
- `continue`는 반복을 끝내지 않고 그 회차만 건너뛴다는 것 — 반복 전체를 종료하는 `break`와 다르다는 것

## 다음에 볼 것
- 딕셔너리(Dict), 문자열(String), 세트(Set), 함수(Function), 모듈(Module)
