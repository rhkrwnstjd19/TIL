# 2026-08-13 — 파이썬 딕셔너리·문자열·함수, 중첩 데이터 필터링

## 오늘 한 일
- 집합(Set)·튜플 이론 학습 — 교집합·합집합·차집합, 빈 집합은 `set()`으로 생성
- 문자열(String) 이론 학습 — 인덱싱·슬라이싱, `f-string`/`.format` 포매팅, 자주 쓰는 메서드, 아스키코드
- 딕셔너리(Dict) 이론 학습 — 키의 제약, `get`/`pop`/`keys`/`values`/`items`
- 함수(Function) 이론 학습 — 매개변수·인자·반환값, 내장함수와 메서드의 차이
- 딕셔너리·리스트가 중첩된 데이터를 다루는 문제를 problem07까지 풀이(게시글 필터링, 유저 권한 판정, 카테고리별 상품 필터링, 품절 주문 판별)

## 배운 점
- 딕셔너리의 키는 불변 자료형만 가능하다는 것 — 정수·실수·문자열·불린·튜플·레인지는 키로 쓸 수 있지만, 리스트처럼 가변 컨테이너를 키로 쓰면 `TypeError`가 난다는 것
- 중첩된 딕셔너리/리스트를 다룰 때는 바깥 반복문 → 안쪽 반복문 순으로 한 단계씩 값을 꺼내는 것이 안전하다는 것 — 구조를 그대로 따라가며 값을 꺼내면 실수가 줄어든다는 것
- 단축 평가(short-circuit evaluation)로 조건문을 단순화할 수 있다는 것 — `A or B`에서 A가 참이면 B는 검사하지 않고, `A and B`에서 A가 거짓이면 B를 검사하지 않는다는 성질을 이용해 여러 조건을 하나로 합칠 수 있다는 것
- 하나의 항목에서 조건을 만족하는 하위 요소가 여러 개일 때 상위 값이 중복 집계되지 않게 하려면, 안쪽 반복문에서 조건이 맞는 순간 `break`로 빠져나와야 한다는 것
- 문자열은 불변(immutable)이라 인덱싱으로 직접 수정할 수 없고, 슬라이싱과 덧셈으로 새 문자열을 만들어야 한다는 것
- 집합의 빈 값은 `{}`가 아니라 `set()`으로 만들어야 한다는 것 — 빈 중괄호는 딕셔너리로 취급되기 때문이라는 것

## 다음에 볼 것
- 모듈(Module)
- problem08(함수), problem09
