# 2026-08-19 — 달팽이 배열, deque로 푸는 스택·큐 문제, 예외처리

## 오늘 한 일
- 달팽이 배열(`snail.ipynb`) 완성 — `5x5` 격자를 1부터 25까지 시계 방향으로 채우는 코드를, 손으로 3칸 넣어보는 단계부터 시작해 반복문으로 일반화
- `collections.deque` 로 스택과 큐를 다루는 법 정리 — `append`/`pop`(오른쪽), `appendleft`/`popleft`(왼쪽)
- 프로그래머스 **올바른 괄호**를 두 가지로 풀이 — `()` 만 있을 때는 카운터로, `[]`·`{}` 까지 확장할 때는 스택으로
- 프로그래머스 **다리를 지나는 트럭** 풀이 — 다리를 `deque([0]*bridge_length)` 로 두고 매 초 한 칸씩 밀어내는 시뮬레이션
- 파이썬 예외처리(`exception_handling.ipynb`) — `try`/`except`/`else`/`finally`, `as` 로 예외 객체 받기, `raise`, 사용자 정의 예외

## 배운 점
- 격자를 돌 때는 **바로 이동하지 말고 이동 후보 `(ni, nj)` 를 먼저 계산**해서 검사한 뒤 확정해야 인덱스 관리가 쉬워진다는 것 — 먼저 움직여버리면 격자를 벗어난 뒤에 되돌려야 해서 코드가 복잡해진다는 것
- 달팽이처럼 방향을 순환시킬 때 `d += 1` 후 `if d == 4: d = 0` 대신 `d = (d + 1) % 4` 한 줄로 처리할 수 있다는 것
- 달팽이는 "격자를 벗어났을 때"만 방향을 바꾸면 안 되고 **"이미 값이 채워져 있을 때"도 방향을 바꿔야** 안쪽으로 감겨 들어간다는 것 — 조건이 `not (격자 안) or snail[ni][nj]` 두 개여야 한다는 것
- 스택과 큐의 차이는 결국 **삽입과 삭제가 같은 쪽이냐 반대쪽이냐**일 뿐이고, 둘 다 `deque` 하나로 만들 수 있다는 것 — 스택은 `append`+`pop`, 큐는 `append`+`popleft`
- 리스트로도 큐를 흉내낼 수는 있지만 `lst.insert(0, x)` 나 `lst.pop(0)` 은 앞의 원소를 전부 밀어야 해서 느릴 수 있다는 것
- 멤버십 검사(`in`)는 리스트에서는 처음부터 훑어서 `O(n)` 이지만 집합(`set`)에서는 `O(1)` 이라 열린 괄호 목록 같은 건 `set` 으로 두는 게 낫다는 것
- 괄호 짝 검사는 `{']':'[', '}':'{', ')':'('}` 처럼 **닫는 괄호 → 여는 괄호 매핑 딕셔너리**를 두면 괄호 종류가 늘어나도 `elif` 를 늘리지 않아도 된다는 것. 그리고 실패 조건이 세 개(짝이 안 맞음 / 스택이 비었는데 닫힘 / 끝났는데 스택에 남음)라는 것
- 괄호 문제에서 카운터 방식(`(` 는 +1, `)` 는 -1)은 합이 0이어도 `)(` 처럼 **중간에 음수가 되는 경우**를 잡지 못하므로, 매 글자마다 음수 검사를 해야 한다는 것
- 다리를 지나는 트럭처럼 "길이가 고정된 통로"는 빈칸을 `0` 으로 채운 큐로 표현하면 시간이 곧 반복 횟수가 되어 계산이 단순해진다는 것
- 예외처리에서 `except:` 로 전부 잡는 것보다 `ValueError`·`ZeroDivisionError` 처럼 **예외 종류를 명시**해야 엉뚱한 오류를 조용히 삼키지 않는다는 것. `else` 는 예외가 없을 때만, `finally` 는 성공·실패와 무관하게 항상 실행된다는 것
- `raise` 로 예외를 일부러 일으킬 수 있고, `Exception` 을 상속해서 사용자 정의 예외를 만들 수 있다는 것

## 다음에 볼 것
- `requests` 로 외부 API 호출하기 (`api/python_api.ipynb` 자료 배포됨)
