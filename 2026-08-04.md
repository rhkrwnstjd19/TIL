# 2026-08-04 — "맛집 도장깨기" AI 추천 기능 (Gemini + Supabase)

## 오늘 한 일
- 전날(08-03) 시작한 "맛집 도장깨기" Next.js 프로젝트를 이어서 개발
- Supabase Auth 기반 로그인/회원가입/로그아웃 서버 액션과 화면 작성
- 맛집 데이터 CRUD(등록/조회/수정/삭제) 및 카테고리·지역 필터, 인기 맛집·지역별 랭킹 조회 구현
- 네이버 검색 API로 맛집 썸네일 이미지 연동(실패 시 이모지 타일로 폴백)
- 마이페이지에 "가볼 맛집"/"다녀온 맛집" 탭 UI 구현
- Google GenAI(Gemini) 로 위치 기반 맛집 추천 기능 구현 — 이날의 마지막 작업

## 배운 점
- **AI 추천 응답을 곧이곧대로 쓰지 않는다는 것** — Gemini에게는 추천 순서와 이유(reason)만 받고, 실제로 화면에 보여줄 이름·주소·평점 등은 항상 우리 DB 값으로 다시 조립한다. 모델이 후보 목록에 없는 가게를 지어내도 매칭에 실패해 자동으로 걸러진다
- **LLM 구조화 출력** — `response_format` 에 JSON 스키마를 지정해 모델 응답을 항상 정해진 필드(name/address/category/reason) 형태로만 받을 수 있다는 것
- **외부 API 연동은 항상 폴백을 전제로 설계한다는 것** — 네이버 이미지 검색 API는 키 미설정이나 요청 실패 시 예외 대신 `null` 을 반환하도록 만들고, 호출부가 그 경우 이모지 타일을 대신 쓰도록 분리했다
- **환경변수 플래그로 개발용 우회 기능을 완전히 격리한다는 것** — OAuth 연동이 끝나지 않은 동안에도 나머지 플로우를 검증할 수 있도록, 고정 테스트 계정 로그인을 별도 환경변수로만 켜고 끄게 만들어 실서비스에서는 흔적 없이 꺼둘 수 있게 했다
- **세션 내 중복 방지 상태관리**: "다시 추천받기"를 눌러도 방금 본 항목이 또 나오는 문제 → 프론트에서 `useRef<Set>` 로 이미 보여준 추천 키를 누적해 다음 요청에서 제외 → 제외 후 후보가 하나도 안 남으면(다 보여줬으면) 처음부터 다시 보여주는 것으로 해결(빈 결과보다 반복 추천이 자연스럽다는 판단)

## 다음에 볼 것
- (전날 노트에 이미 "맛집 도장깨기 이어가기"가 계획으로 남아 있었고, 이날 작업이 그 이어가기였음 — 이 시점 기준 별도 TODO 표시는 없음)
